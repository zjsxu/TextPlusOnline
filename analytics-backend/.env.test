# Test Environment - No External Dependencies
NODE_ENV=test
PORT=3001
HOST=0.0.0.0

# Mock Database URLs (will be handled by mock services)
DATABASE_URL=mock://localhost
REDIS_URL=mock://localhost
INFLUXDB_URL=mock://localhost
INFLUXDB_TOKEN=test-token
INFLUXDB_ORG=test
INFLUXDB_BUCKET=test

# Security
JWT_SECRET=test-jwt-secret
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# CORS
CORS_ORIGIN=*

# Logging
LOG_LEVEL=info